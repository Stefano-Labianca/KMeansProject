{
  // Nomi delle colonne
  "columnsName": ["outlook", "temperature", "umidity", "wind", "play"],

  // Contiene tutti i cluster calcolati
  "clusters": [
    {
      // Centroide
      "middle": {
        "middleData": ["rain", 11.135, "normal", "weak", "yes"]
      },

      // Contiene tutti gli "esempi" piu' vicini al centroide scelto dove
      // ogni oggetto contiene i dati "dell'esempio" e la sua distanza
      // dal centroide
      "examples": [
        {
          "exampleData": ["rain", 13.0, "high", "weak", "yes"],
          "distance": 1.061
        }
      ],

      // Distanza media degli "esempi" dal centroide
      "avgDistance": 10.0
    }
  ],

  // Numero di iterazioni dell'algoritmo
  "iterations": 10
}
